import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
`

const PageLinks = (props) => {
  const { labels, path } = props
  return(
    <Wrapper>
    </Wrapper>
  )
}

export default PageLinks